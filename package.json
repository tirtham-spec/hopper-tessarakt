{
  "name": "sabika-dhf-project",
  "version": "1.0.0",
  "private": true,
  "description": "DeHaat Honest Farms — Category Creation & Scale Acceleration. Interactive strategy deck.",
  "scripts": {
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy",
    "serve": "npx http-server ./site -p 8080 -c-1",
    "check": "node tools/shoot-deck.js /tmp/deckshots && node tools/check-crops.js && node tools/check-scroll.js",
    "build:deck": "node tools/build-html-deck.js && node tools/bundle-deck.mjs DHF-Honest-Farms-Deck.html"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  },
  "dependencies": {
    "wrangler": "^4.123.0"
  }
}
